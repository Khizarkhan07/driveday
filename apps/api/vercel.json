{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npx tsc -p ../../packages/shared-types/tsconfig.json && npm run build",
  "functions": {
    "api/index.ts": { "memory": 1024, "maxDuration": 30 }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
